// Extension data
export const extensionData = {
    id: 'faultytoolbox',
    name: 'Faulty Tool Box',
};

// Commands on beta
export const betaCommands = {
    test: `${extensionData.id}.sayHi`,
    stop: `${extensionData.id}.sayStop`,
};

// File creation commands
export const fileCreatorCommands = {};

// Namespace commands
export const namespaceCommands = {};

// Nuget commands
export const NugetCommands = {};

// Dotnet commands
export const DotnetCommands = {};
